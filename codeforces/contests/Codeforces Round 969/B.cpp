#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n, m;
    cin >> n >> m;
    char temp1;
    int temp2, temp3;
    vector<int> arr(n);
    for (int x = 0; x < n; x++) {
        cin >> arr[x];
    }
    for (int x = 0; x < m; x++) {
        cin >> temp1 >> temp2 >> temp3;
        
    }
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
}
