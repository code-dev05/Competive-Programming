#include <bits/stdc++.h>

using namespace std;

int solve(string& s, int n) {
    int i = 0, j = 0;
    map<int, int> m;
    int count = 0;
    while (j < n) {
        m[j]++;
        if (m[j] == 2) {
            
        }
    }
}

int main() {
    string s;
    cin >> s;
    int n = s.size();
    cout << solve(s, n) << endl;
}